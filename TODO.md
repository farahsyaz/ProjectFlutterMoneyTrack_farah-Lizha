## TODO - Dashboard fix

- [ ] Fix `_buildEmptyState` in `lib/screen/dashboard.dart` to resolve syntax + widget type errors.
- [ ] Replace deprecated `withOpacity(...)` calls with `withValues(alpha: ...)` in `lib/screen/dashboard.dart`.
- [ ] Run `flutter analyze` and confirm no remaining errors in `dashboard.dart`.

